git_url("git@gitee.com:Sesn/testcertificates.git")

storage_mode("git") # 存储方式
git_branch("master")  # git分支名称

# 默认的Profile类型, 可以为: appstore, adhoc, enterprise or development
type("development")

# bundleId，可以填多个bundleId，如App内包含 extension
app_identifier(["com.dt.qw"])
ENV["MATCH_PASSWORD"] = "abc123" # 导出和打开 .p12文件的密码

username("zhang841766048@gmail.com") # 苹果ID账号
ENV["FASTLANE_PASSWORD"] = "Yu-502049336" # 需要的密码
ENV['MATCH_KEYCHAIN_PASSWORD'] = 'Yu-502049336' # 需要的密码